import QtQuick 2.1;

Item {
    width: implicitWidth;
    height: implicitHeight;
    implicitWidth: size;
    implicitHeight: size;

    property real  size  : 10;
    property color color : "magenta";
}
